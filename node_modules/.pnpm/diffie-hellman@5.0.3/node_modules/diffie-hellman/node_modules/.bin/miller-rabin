#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -z "$NODE_PATH" ]; then
  export NODE_PATH="/home/ismoilovdev/Desktop/Project/website/node_modules/.pnpm/node_modules"
else
  export NODE_PATH="$NODE_PATH:/home/ismoilovdev/Desktop/Project/website/node_modules/.pnpm/node_modules"
fi
if [ -x "$basedir/node" ]; then
  exec "$basedir/node"  "$basedir/../../../../../miller-rabin@4.0.1/node_modules/miller-rabin/bin/miller-rabin" "$@"
else
  exec node  "$basedir/../../../../../miller-rabin@4.0.1/node_modules/miller-rabin/bin/miller-rabin" "$@"
fi
